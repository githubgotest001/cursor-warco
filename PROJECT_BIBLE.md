# UMBRELLA 4365 · 项目设定集（PROJECT BIBLE）

> **这份文档是什么**：本站的「设定集 + 运营手册」——记录网站的世界观、风格、调性、固定文案、视觉系统、内容规范与后台用法。供两类读者使用：
> ① **人**（站长本人，隔了几个月回来忘了当初怎么定的）；② **AI**（新会话没有记忆，动手改站或写档案前先读这份，防止跑调）。
>
> **维护纪律**：凡是「拍板过的决定」（文体、命名、颜色、禁忌）都应落在这里；改了拍板项，同步更新本文档。技术实现细节不在此重复——见文档地图。
>
> 最后更新：2026-08-29

---

## 0. 文档地图（谁管什么）

| 文档 | 职责 |
| --- | --- |
| `PROJECT_BIBLE.md`（本文档） | 设定、风格、调性、固定文案、后台使用、已定决策与禁忌 |
| `README.md` | 技术说明书：功能清单、数据模型、API、环境变量、SEO/GEO 实现 |
| `DEPLOY.md` | 云部署手册：Nginx + HTTPS + systemd + 备份 + 验收清单 |
| `GROWTH.md` | 增长作战手册：站长平台接入、社区分发、外链建设、目标关键词与复盘节奏 |
| `AUTOMATION.md` | 自动化手册：哨兵 / 侦察 / 战报 / 扇出的 agent 编辑部规程与人工闸口 |
| `.cursor/skills/warco-chronicler/`（含 `references/fields-and-voice.md`） | 档案撰写执行规范：逐字段规则、正史/野史文风范例、术语纪律 |
| `.cursor/skills/warco-scout/`（含 `references/sources.md`） | 档案侦察执行规范：信源分级清单、查缺/增量两种搜集模式 |
| `.cursor/skills/warco-dispatch/` | 每周战报汇编规范：战报体、固定结构、期号纪律 |
| `.cursor/skills/warco-herald/` | 分发文案规范：各渠道口音、不教唆红线、链接纪律 |
| `drafts/README.md` | drafts/ 目录索引：待办 / 工作草稿 / 渲染源 / 决策存档四类的清单与处置纪律 |

人类新读者从 `README.md` 顶部的「文档地图」进；AI 动手改站前先读本文档第 10 节「已定决策与禁忌」。

文体细则以 skill 的 `fields-and-voice.md` 为准，本文档只收录「为什么这么定」和速查摘要。

---

## 1. 网站身份卡

| 项 | 内容 |
| --- | --- |
| 站名 | UMBRELLA 4365 |
| 域名 | [umbrella4365.com](https://umbrella4365.com) |
| 定位一句话 | 以 Cursor 为坐标的「战地纪实」网站：一条正史（官方档案）、一条野史（场外情报），沿一条垂直时间脊柱由新到旧记录 AI 编程纪元的光与影 |
| 站训（主标语） | **AI一日，人间一年**（2026-08-28 定稿，替换更早的主标语） |
| 英文刊名 | UMBRELLA 4365 · CURSOR FRONTLINE CHRONICLE |
| 刊眉 | VOL.001 · 蜂巢档案 HIVE ARCHIVE |
| 署名 | FILED BY · **CURSOR WARCO** ｜ 战地记者（Warco = War Correspondent 的行话缩写） |
| 立场声明 | 不站队、不批判、仅作记录；野史含演绎，不构成立场 |
| 上线 | 2026-08-27 全栈上线（前台 + 后台 + 部署文档） |

**站名释义**：Umbrella 取自《生化危机》的保护伞公司；4365 与服务默认端口一致，字面上呼应站训里的「一年 365 天」。（取名的完整心证当时没留档——若站长记得本源，请回来补这一行。）

---

## 2. 世界观设定（Canon）

### 2.1 母题

**保护伞公司 × 生化危机 × 战地新闻**。整个网站假装自己是保护伞公司的「蜂巢档案库」，由中央 AI「RED QUEEN（红后）」维护档案，一位署名 Cursor Warco 的战地记者向档案库供稿。AI 编程纪元被叙述成一场「疫情」：模型是毒株，扩散是大流行，Cursor 的历史是疫情编年史。

### 2.2 意象辞典

| 意象 | 出现位置 | 含义 |
| --- | --- | --- |
| RED QUEEN（红后） | 报头「RED QUEEN ONLINE」、后台名「RED QUEEN TERMINAL」、野史文体、报错文案「RED QUEEN OFFLINE · 档案库连接中断」 | 档案库的系统人格；野史每条档案都假装是它吐出的记录单 |
| 蜂巢 HIVE | 刊眉「蜂巢档案」、全站六边形背景纹理 | 保护伞的地下实验室；档案库本体 |
| 时间脊柱 | 前台中轴红线 | 时间轴主干，最新在上；桌面端居中，移动端靠左 |
| 无菌区 / 污染区 | 前台左右背景渐变、卡片配色 | 左＝正史（浅色档案纸），右＝野史（深色污染档案） |
| 毒株原点 STRAIN ZERO | 时间轴最底端 | 一切的起点（Anysphere 创立）；文案「毒 株 原 点 / STRAIN ZERO · 一切从这里开始」 |
| 年份检查站 | 每个年份分隔处的菱形闸门 + 幽灵大字年份 | 时间轴上的「闸门」，两侧标注该年的纪元阶段 |
| CLEARANCE LV.4 | 档案调阅弹层标题栏、后台登录页「需要 LV.4 权限」 | 档案密级设定；管理员即持有 LV.4 权限的人 |
| 编号体系 | 卡片右上角与弹层 | 正史 `A-<id 补零 3 位>`，野史 `X-<id>`；战地手记固定 `N-000` |
| LIVE 脉冲 | 时间轴顶端 | 「未完待续 · 记录进行中」＋「LIVE · 战地记者在线」，闪烁光标即 Cursor 本尊 |
| 野史印章 | 野史卡片右上角手写体「野史」斜印 | 用手写字体盖章，标记非官方档案 |

### 2.3 双线：正史 / 野史

- **正史**（`side: main`，脊柱之左，浅色档案纸）：台面之上的官方大事记——融资、发布、并购、故障通报。
- **野史**（`side: dark`，脊柱之右，深色污染档案）：同一时刻的暗面——漏洞、套利、事故、传闻、玩梗，多采自封闭频道与场外线报。
- **双线互文是本站的精髓**：同一事件官方发布（正史）与场外玩法（野史）同日各立一条，两种文体的温差本身就是叙事。

### 2.4 纪元表（PHASES）

年份检查站两侧的阶段文案，写死在 `public/index.html` 的 `PHASES` 常量里；**新增年份时须同步补一行**：

| 年份 | 正史侧（左） | 野史侧（右） |
| --- | --- | --- |
| 2022 | PHASE-0 始祖毒株 · PROGENITOR | 暗面无记录 |
| 2023 | PHASE-I 初次泄漏 · OUTBREAK | 暗面微弱杂音 |
| 2024 | PHASE-II 城市扩散 · SPREAD | 暗面异常增殖 |
| 2025 | PHASE-III 全球大流行 · PANDEMIC | 变异体横行 · MUTATION |
| 2026 | PHASE-IV 吞并纪元 · ASSIMILATION | 暗面信号追踪中… |
| 未登记年份 | PHASE-? 观测中 · <年份> | 暗面信号追踪中… |

### 2.5 钦定文案（改动需慎重）

**发刊词**（首页时间轴顶部，`index.html` 内联）：

> 大模型纪元，一夜巨变、一周洗牌。本刊以 Cursor 为坐标，由新到旧铺开两条战线：脊柱之左，是台面上的正史；脊柱之右，是同一时刻的暗面野史——漏洞、套利、事故与传闻，多采自封闭频道与场外信源。记者不站队、不批判、仅作记录；正因热爱，才如实记下它的光与影。

（移动端把「脊柱之左/右」自动替换为「浅色/深色档案」。）

**战地手记**（记者私话，全站唯一的「人声区」，不受术语纪律约束；入口藏在发刊词末尾「附 · 战地手记 »」和报头署名，编号 N-000 · OFF THE RECORD，锚点 `#memo`）：

> **光与影之外**
>
> 档案之外，容我摘下记者的帽子，说几句私话——
>
> 高级模型愈强、愈贵，需求也就愈烈；钻漏洞「白嫖」，于是成了公开的秘密。
>
> 可漏洞随时会被补上。无论个人还是团队，都被困在一个随时关闭的窗口里赶工——为「用掉」模型而用，动作变形，来不及深思熟虑。
>
> 荒诞正在于此：紧绷着调用最强的模型，产出未必胜过从容时用一个普通模型。
>
> 舍本逐末，莫过于此。愿这片环境早日回归正常——真正稀缺的从来不是模型额度，而是从容思考的心力。
>
> —— Cursor Warco · 战地记者 · WAR CORRESPONDENT

**页脚免责声明**：

> UMBRELLA 4365 ARCHIVE · 基于公开报道与场外情报整理 · 野史含演绎 仅作记录 不构成立场
> 记者本人是 Cursor 重度用户 · umbrella4365.com · RSS · llms.txt

**弹层脚注**：`UMBRELLA 4365 ARCHIVE · 仅作记录 · 不构成立场`

**后台登录页格言**：`SYSTEM: 保持冷静，如实记录。`

### 2.6 记者人设

Cursor Warco，战地记者。核心身份感：**是 Cursor 重度用户，正因热爱才如实记录光与影**；不是通稿写手，不是喷子，不站队。写任何档案、任何站内文案时都带着这个身份。记者的「私人观点」只允许出现在战地手记（N-000）一处。

---

## 3. 视觉设计系统

### 3.1 色板（CSS 变量定义在 `public/index.html`）

| 变量 | 色值 | 语义 |
| --- | --- | --- |
| `--ink` | `#0a0b0e` | 蜂巢夜幕（全站底色） |
| `--ink-soft` | `#12141a` | 底色亮一档 |
| `--umb` | `#e0242e` | 保护伞红（主题色） |
| `--umb-hi` | `#ff4a52` | 保护伞红·亮（强调、野史文字高亮） |
| `--umb-dim` | `#7d1a20` | 保护伞红·暗 |
| `--bone` | `#e9e6df` | 骨白（正文、正史侧） |
| `--bone-dim` | `#9aa0a8` | 骨白·暗（次要文字） |
| `--steel` | `#58606e` | 钢灰 |
| （无变量） | `#edeae2 → #d9d5c9` 渐变 | 正史卡「无菌区档案纸」底色 |
| （无变量） | `#a01820` | 档案纸上的暗红（正史卡日期、标签、链接） |
| （无变量） | `#180c10 → #100a0e` 渐变 | 野史卡「污染区」底色 |
| （无变量） | `#ffd9d4` / `#c7a8a6` | 野史卡标题 / 正文 |

红白黑三色是全站基调；除 glitch 动效瞬间的青紫残影外，不引入第四种颜色倾向。

### 3.2 字体

| 变量 | 字体 | 用途 |
| --- | --- | --- |
| `--serif` | Noto Serif SC → 系统衬线（宋体/Songti SC）回退 | 报头标题、卡片标题、弹层标题——「特稿」气质 |
| `--sans` | Noto Sans SC → 苹方/雅黑回退 | 正文 |
| `--hand` | Ma Shan Zheng（楷体风手写，自托管子集） | 野史印章、战地手记签名——「人手盖章」气质 |
| `--mono` | JetBrains Mono（自托管 latin 子集） | 日期、编号、元信息、按钮、全部后台——「终端」气质 |

2026-08-29 起不再引 Google Fonts（境内不可达，阻塞渲染拖垮百度页面体验分）：中文字体直接走
系统字体栈（本机装有 Noto 系列的用户自动优先）；mono 与手写体的子集 woff2 自托管在
`public/fonts/`（共 34KB，手写体只含「野史」印章与签名字符）。后台只用 sans + mono。

### 3.3 纹理与氛围

- **噪点**：全屏 fractalNoise SVG，opacity .05，盖在最上层（胶片颗粒感）。
- **蜂巢**：全屏六边形描边 SVG 平铺，极淡（.045）。
- **左右分区**：stage 背景左侧微白光、右侧微红光；右半屏另叠横向红色扫描线。
- 分享图 `public/og.png`（1200×630）延续同一套语言，渲染源是 `drafts/og-render.html`（浏览器打开截图导出）。
- 站标 `public/logo.svg`：保护伞红白伞面 × Cursor 六边形 × 中央光标。

### 3.4 版式

- 时间树容器 `max-width: 1180px`，中轴红色脊柱带辉光；卡片宽 `min(400px, 100%)`。
- 事件节点是**菱形**（旋转 45° 的方块）：正史实心骨白，野史空心红边——与图例一致。
- 卡片到脊柱的连线长度在 `[40, 112, 72]px` 三档循环，制造参差；左右两侧相邻档案会互相「插空」（`tuck` 类，上移 48px），排版更紧凑。
- 正史卡文字右对齐（面向脊柱），右侧红色竖边；野史卡红色虚线边框 + 「野史」手写印章。
- **移动端（≤860px）**：脊柱移到左侧 26px，单栏布局，全部卡片靠左；正史卡改左侧红边、文字左对齐；方位文案自动从「脊柱之左/右」切换为「浅色/深色档案」。

### 3.5 动效清单

| 动效 | 位置 | 说明 |
| --- | --- | --- |
| 逐字浮现 | 报头主标语 | 每字 55ms 步进上浮 |
| 光标闪烁 | LIVE 脉冲、报头 RED QUEEN 圆点 | 打字光标 = Cursor 的自指 |
| 卡片入场 | 时间轴滚动 | IntersectionObserver，正史从左滑入、野史从右滑入（移动端上浮） |
| glitch 故障 | 野史卡标题 hover | 0.34s 红青紫错位残影，闪两次 |
| breathe 呼吸 | 年份闸门、毒株原点 logo | 红色辉光呼吸 |
| 进度条 | 页面顶部 | 骨白→红渐变，随滚动增长 |

无障碍：`prefers-reduced-motion` 时全部动画关闭；入场动画通过 `<html class="js">` 门控，无 JS 环境（爬虫）内容直接可见。

---

## 4. 内容体系（写什么、怎么写）

### 4.1 数据字段

一条档案 = `side / date / tag / title / summary / detail / image / series / source / front`（+自动的 `created_at / updated_at`）。逐字段精确规范见 `warco-chronicler/references/fields-and-voice.md`，速记：

- `date` 记**事件发生/官宣日**，不是写作日；`YYYY-MM-DD`。
- `title` ≤ 20 字、信息优先（线索列表里只显示日期+标题）。
- `summary` 一段 60–90 字（时间轴卡片）；`detail` 空行分段（调阅弹层）。
- `image` 默认留空，**图片永远由站长自备**。
- `source` 填一条最硬的信源 URL。
- `front` 战区代号（2026-08-30 起）：**默认空 = Cursor 主战线**；只有主角不是 Cursor 的邻圈档案才填
  （`claude-code / codex / copilot / windsurf / cn-tools / model-labs`，名册登记在 `server.js` 与
  `index.html` 的 `FRONTS`）。扩战区纪律：Cursor 是坐标不是被稀释的对象，邻圈只收「改变格局 / 钱 /
  大批用户体验」量级的事件，宁缺毋滥。

除档案外另有**刊物**（`posts` 表）：`kind=weekly` 每周战报（`/w/期号`，文体与结构见 warco-dispatch skill）、
`kind=feature` 专题特稿（`/t/slug`，特稿体长文）。刊物走后台「编辑部」管理，draft 态不对外，与档案合流进 RSS。

### 4.2 文体（2026-08-28 定稿，不再重开讨论）

- **正史 = 杂志特稿体**：彭博商业周刊腔。先事实（时间/主体/数字/引语），反讽藏在事实的排列里，收束句冷静有余味；禁网络梗、禁感叹号情绪、禁「战地记者按」。
- **野史 = 红后终端体**：RED QUEEN 记录单。summary 用「现象：/取证：/玩法：…」字段化短句拼装，以「系统备注：」冷反讽收尾；detail 固定三段（`记录时间 …` 白描 → 解析/核查 → `系统备注：` 收束）。核心事件必须真实，演绎只作用于语气。
- 当时对比过四个备选方向，决策过程存档在 `drafts/style-samples-2026-08-28.md`；定稿后库内全部档案已按新文体全量重写。
- **不做文字交叉引用、不做跨档案时间算术**（2026-08-29 定稿）：正文不写「（见 YYYY-MM-DD 档案）」「另见野史档案」式指路句，不写「窗口共 N 天」「N 天/周/月后」式依赖两条档案日期的计算——日期可能修订，硬编码会连环过期；关联与窗口跨度交给 series 机制。叙事纵深用「随后 / 后来」或年级别粗粒度（「三年后」可用）；单一事件内部的时长（「停机 19 天」）属报道事实，不受限。

### 4.3 tag 词库（2–4 字中文，可扩充但保持性格）

- 正史常用：起源、首发、融资、并购、里程碑、发布、整合、纪元、惊变、军备、登顶、夺榜、故障、动荡、结盟、上市、变阵、破壁、进化、平价、平替、反攻、进场、双子、首秀、点火、断供
- 野史常用：冷眼、破防、造词、罢工、事故、套利 / 重套利 / 极限套利（按量级分级）、**灭活**（灭活档案固定用此 tag）、众怒、谍战、删库、围城、玩梗、扒皮、漏洞、情报、封锁

### 4.4 事件线索（series）

同一故事线的多条档案填同一个 `series`，前台自动串成时间线并计算窗口跨度。规则：

- 新开线索门槛：已有或可预期 **≥2 条**档案，孤条不挂。
- **检出/灭活一拆二**（2026-08-29 定稿，替代旧词开闸/拉闸）：有生命周期的羊毛/漏洞事件，检出（玩法被观测到）、灭活（玩法被官方废掉）各立一档同挂一个 series；标题格式 `<线索名>·检出：<玩法>` / `<线索名>·灭活：<结果>`（标题与正文都不带窗口天数，不写回链——串联与跨度由 series 自动生成）。玩法仍存活时先不挂 series，标注 `（存活）`，灭活后回头补挂改题。可选扩展态：`·变异：`（削弱后改版存活）、`·复燃：`（死灰复燃）。
- 现役线索（截至 2026-08-29 快照）：`融资阶梯`、`模型军备`、`自研模型线`、`火箭并购案`、`定价攻防`、`AI 失控档案`、`供给线`（模型供应商对 Cursor 的进与退），以及检出/灭活成对线索 `临期锁额`、`Team 席位差`、`假焚诀`。

### 4.5 术语纪律（记录者口音）

原则：**叙述词汇用冷的情报术语；被记录的黑话保留但加「」降格为语言样本**。核心对照（全表见 fields-and-voice.md）：

民间→场外｜群聊→封闭频道｜圈内→情报网内｜爆料→线报｜羊毛党→套利者｜薅羊毛→套利｜发糖→补给/放闸｜社区传闻→未具名信源。「拉闸」「放闸」「回血」视为已归化的系统词，**仅限叙述动词**（如「被出口管制拉闸」「官方放闸」）；立档格式一律用「检出/灭活」。战地手记是唯一豁免区。

### 4.6 查证纪律

- 正史的日期、金额、轮次、估值、版本号**必须查证**；查不到就模糊化（「约」「据公开报道」），宁可含糊不可编造。
- 野史核心事件必须真实存在（哪怕信源是群聊截图/Reddit/HN），演绎只作用于语气。
- 越劲爆越要交叉验证（先例：「SpaceX 600 亿收购 Cursor」查证后为真，有 SEC 文件）。
- 信源分级 L0–L4 及各家的坑见 `warco-scout/references/sources.md`；聚合站只当目录、严禁直接引为事实。

---

## 5. 内容生产流水线（2026-08-30 起为 agent 编辑部形态，详见 AUTOMATION.md）

```
tools/sentinel.js 哨兵（定时轮询信源，diff 新信号 → data/sentinel-queue.md + TG 提醒）
   → warco-scout 侦察（消化队列 / 查缺 / 增量 → 交叉查证 → 按 chronicler 规范成稿）
   → drafts/*.md 或 线上收件箱（POST /api/drafts 草稿态，含「核查要点」）
   → 【人工闸口】站长在后台「收件箱」逐条审核 → 一键发布为档案（自动触发 SEO 重建 + 百度推送）
   → warco-herald 扇出（各渠道文案，站长粘贴发布）
每周：warco-dispatch 汇编战报草稿 → 站长在「编辑部」审阅发布 /w/:issue
```

- **录入永远是站长的动作**。AI 默认只交草稿/字段代码块，不写库；仅当明确说「帮我录」时才 `POST <线上地址>/api/events`（请求头 `X-Admin-Key`，地址与密钥在 `data/remote.json`）——**录到线上，不写本地库**。
- **线上库是唯一真源，本地库只是只读镜像**：scout/分析类工作第一步 `node sync.js pull` 刷新镜像再读（`data/chronicle.db` 只读打开）；改动回写走线上 API 逐条（id 稳定、自动触发 SEO 缓存重建与百度推送），图片用 `node sync.js push-image <文件>` 上传。（seed.js 种子快照已于 2026-08-30 退役删除——静态快照必然过期，且对 AI 分析构成干扰源。）
- 单条新闻直接说「写一条正史/野史」→ 走 warco-chronicler；批量搜集/查缺补漏 → 走 warco-scout。
- 配图：外网截图被反爬拦截时，chronicler 可用本地渲染生成「官方公告存证卡」（做法同 `drafts/og-render.html`：写一个定尺寸 HTML 本地渲染后截图）。
- `drafts/` 内容物与处置纪律见 **[drafts/README.md](drafts/README.md)**（待办 / 工作草稿 / 渲染源 / 决策存档四类索引，本文档不再重复维护清单）。

---

## 6. 后台使用手册（RED QUEEN TERMINAL）

### 6.1 入口与登录

- 入口路径是**隐藏的**：`/admin`、`/admin.html` 恒为 404（防嗅探）。真实路径 = 环境变量 `ADMIN_PATH`，未设置时自动生成随机值，存在 `data/config.json`、打印在启动日志。
- 登录：输入管理密钥（`ADMIN_KEY`，本地默认 `redqueen-4365`，生产环境已改为强随机值）。密钥校验通过后存 localStorage（键 `umb4365_key`），下次免登录；「注销」清除。
- 忘了入口/密钥：服务器上 `systemctl cat umbrella4365` 看 Environment 行，或 `journalctl -u umbrella4365 -n 20` 看启动日志；自动生成的路径在 `data/config.json`。
- **防爆破**：同一 IP 15 分钟内密钥错 5 次封禁 15 分钟（登录与全部写接口共用计数，内存态，重启即清）。生产必须 `TRUST_PROXY=1`，否则所有人共享 Nginx 的 IP 会误伤全站。

### 6.2 档案管理标签页

- **列表**：筛选（全部/正史/野史）＋搜索框（搜标题/摘要/详情/标签/线索，300ms 防抖）＋每页 10/20/50＋分页器；点列头「日期/创建/修改」切换排序（再点切升降序）。标题行会带 `[图]` `[源]` 标记和 tag/series 徽标；点 series 徽标＝按线索筛选。
- **新建/编辑**：弹层表单。日期/标题/摘要必填；tag 与 series 输入框带 datalist 下拉（自动列出已用过的值，series 还显示节点数与首末日期），也可手输新值。
- **图片**：可直接粘贴 URL，或「选择图片上传」（png/jpg/gif/webp/svg，≤10MB，走 base64 JSON 上传落到 `public/uploads/`），带即时预览。
- **删除**：有 confirm 确认，不可恢复（toast 文案「档案已销毁」）。

### 6.3 访客监控标签页

- 顶部卡片：今日 PV/UV、累计 PV/UV；可切换最近 7/14/30/90 天。
- 四个面板：每日访问趋势条形图、热门路径、**嗅探记录（4xx 异常请求，谁在扫 `/admin`、`/wp-admin` 一目了然）**、来源 Referer。
- 最近访问明细：时间/路径/状态码/访客（IP 哈希前 8 位）/客户端（UA 解析成「Chrome · Windows」「爬虫 / 机器人」等），每页 30 条。

### 6.4 系统标签页（2026-08-30 起）

- **SEO 接入配置**：百度推送 token、百度/Google/Bing 站长验证码，存服务器 `data/config.json`，
  **保存即生效免重启**；非空值优先于同名环境变量，清空回落。附「测试百度推送」按钮
  （把首页推一次百度，直接显示 success/remain 或错误信息，验证 token 链路）。
- **运维速查**：部署更新、看日志、重启、备份、`sync.js` 等常用指令的展示与一键复制
  （命令与 `DEPLOY.md` 保持一致，改部署路径时两处同步）。
- **频道与支援链接**：打赏 / TG / 公众号 / X / 邮箱（前台「⛨ 支援本刊」弹层，配了才显示）
  与「补给线」赞助位（文案 + 链接都填才上墙，输出带 `rel=sponsored`）。
- **一键部署**（2026-08-30 起）：「▲ 部署更新」按钮 = `git pull --ff-only` → 语法自检 → systemd 自动重启，
  面板显示当前 git 版本、部署日志与重启探活。三道保险：只快进拉取、自检不过拒绝重启（旧版继续跑）、
  非 systemd 环境只拉取不重启。涉及一次性迁移脚本 / 环境变量的大版本仍走 SSH。

### 6.5 收件箱 / 编辑部 / 补给线（2026-08-30 起）

- **收件箱**：上半是草稿（侦察 agent 投稿，带「核查要点」，不入正册）——「审核发布」打开档案表单
  预填草稿内容，改完点「发 布 档 案」即转正；「驳回」保留记录不发布。下半是读者线报（前台匿名投递），
  标已读 / 删除。标签页角标 = 待审草稿 + 未读线报数。
- **编辑部**：战报与特稿的建稿 / 编辑 / 发布 / 转草稿。战报期号自动递增建议；「转草稿」会让已发布页
  404，被收录后慎用。
- **补给线**：作战室「模型补给表」的行编辑（模型 / 厂商 / 覆盖 / 价格 / 额度 / 状态 / 排序）；
  断供倒计时类用 `watch` 观察态，撤出用 `removed`（划线显示）。
- 设计边界（不用担心它撑爆或泄露）：静态资源命中不落库（4xx 一律记录）；同 IP 同路径 60 秒去重；内存攒 50 条或 5 秒批量落库；仅保留 90 天、总量上限 20 万行；**IP 只存加盐哈希不落明文**（盐在 `data/config.json`），路径/UA/Referer 截断 200 字符且渲染全转义（防存储型 XSS）。

---

## 7. 技术速查（详见 README.md / DEPLOY.md）

- **栈**：零 npm 依赖，`node:http` + `node:sqlite`，Node ≥ 22.13；默认端口 4365。`node sync.js pull` 从线上拉镜像建库，`node server.js` 启动。
- **文件地图**：`server.js`（静态资源 + REST API + SSR/SEO + 上传 + 鉴权 + 访问记录，单文件）｜`sync.js`（线上 ⇄ 本地同步：pull 镜像 / push-image 传图）｜`data/chronicle.db`（SQLite，WAL，gitignore，本地为只读镜像）｜`data/config.json`（后台路径 + IP 盐 + SEO 接入配置）｜`data/remote.json`（sync.js 的线上地址与密钥，gitignore）｜`public/index.html`（前台，样式脚本全内联）｜`public/admin.html`（后台）｜`public/uploads/`（图片，gitignore）。
- **API**：读公开（`GET /api/events`、`/api/events/:id`、`/api/meta`、`/api/supply`；`POST /api/tips` 公开限速投递），写需 `X-Admin-Key`（events / posts / drafts / tips 管理 / supply 写 / upload / stats / settings；`POST /api/drafts/:id/publish` 为审核发布）。
- **SEO/GEO 已内置**：首页 SSR 直出＋`window.__EVENTS__` 内联、档案独立页 `/ev/:id`（含 JSON-LD、翻页、线索内链）、robots/sitemap/RSS/`llms.txt`/`llms-full.txt`、gzip+ETag、动态产物内存缓存（增删改自动失效）。分享定位：独立页 `/ev/<id>`、站内锚点 `#ev-<id>`、手记 `#memo`、线报 `#tip`、支援 `#support`。
- **情报站页面（2026-08-30 起）**：作战室 `/warroom`（存活窗口 / 最近灭活 / 补给表 / 战线状态，5 分钟缓存）、刊物 `/w`·`/w/:issue`·`/t/:slug`、数据页 `/d/versions`·`/d/funding`（curated 常量在 server.js，更新要补行）·`/d/windows`（全自动推导）。全部进 sitemap 与 llms.txt。
- **生产架构**：`用户 → Nginx（443，HTTPS 终止，www 301 到裸域）→ Node（127.0.0.1:4365）→ SQLite`；systemd 低权限用户 `umbrella` 守护；每日 4 点 cron 备份 `data/` + `uploads/` 保留 30 份。

---

## 8. 数据事实（截至 2026-08-29）

- 档案规模：**89 条（正史 51 / 野史 38）**——2026-08-28 全量改版 84 条 + 2026-08-29 增补 5 条（供给线双档、断供次日野史、9 月双档查缺），按日期升序、id 即时间序（以线上库实时数据为准，`node sync.js pull` 后本地可查）。
- 线索分布：模型军备 12、融资阶梯 7、火箭并购案 7、自研模型线 7、AI 失控档案 3、定价攻防 3、供给线 2、临期锁额 2、Team 席位差 2、假焚诀 2。
- 本地仓库通常没有 `data/`（gitignore）；**线上数据库是唯一真源**，本地 `data/chronicle.db` 由 `node sync.js pull` 生成只读镜像。种子快照体系（seed.js + 全删全增）已于 2026-08-30 退役。
- 时间轴叙事骨架（帮助快速找感觉）：2022 Anysphere 创立（毒株原点）→ 2023 Cursor 上线、「套壳」冷眼、OpenAI 领投种子轮 → 2024 Claude 3.5 点火、A 轮、小孩姐破防、收购 Supermaven → 2025 B/C/D 轮、2.0 与 CTO 离席、客服 Sam 幻觉事故 → 2026 SpaceX 600 亿收购、Grok Bot 下放、盛夏故障潮、各路套利窗口的检出与灭活。

---

## 9. 项目大事记

| 日期 | 事件 |
| --- | --- |
| 2026-08-27 | 全栈上线：双线时间树前台 + SQLite 后台 + 部署文档；同日连发：访客监控、战地手记、事件线索与信源体系、后台分页排序、SEO/GEO（SSR + `/ev/:id` + robots/sitemap/RSS/llms.txt） |
| 2026-08-29 | SEO/GEO 强化：聚合着陆页矩阵（`/s/:slug` 线索页 ×10 + `/y/:year` 年份页 + `/about`，slug 与导语配置在 `server.js` `SERIES_PAGES`）、移除 Google Fonts 改字体子集自托管、首页 head 动态化 + title 补搜索词、百度主动推送钩子与站长验证 meta（环境变量）、brotli、BreadcrumbList、favicon 全家桶（`drafts/icon-render.html` 渲染源）；新增 `GROWTH.md` 增长作战手册 |
| 2026-08-30 | 后台新增「系统」标签页：SEO 接入配置（推送 token / 三家验证码）挪进 `data/config.json` 后台管理、保存即生效免重启（环境变量降为兜底），附百度推送测试按钮与运维指令速查；新增 `sync.js` 同步工具，确立「线上库唯一真源、本地只读镜像、回写走 API」工作流；**seed.js 种子快照体系退役删除**（静态快照必然过期、对 AI 分析构成干扰，本地建库改由 `sync.js pull` 承担） |
| 2026-08-30 | **情报站升级（档案馆 → 情报站）**：红后作战室 `/warroom`（存活窗口 / 最近灭活 / 模型补给表 / 战线状态镜像）、战报周刊与特稿（`posts` 表 + `/w`·`/t` 页 + 后台「编辑部」）、草稿收件箱（`drafts` 表 + 后台审核发布流）、读者线报（前台匿名投递 + 后台查阅）、三张数据页 `/d/versions`·`/d/funding`·`/d/windows`、`front` 战区字段（扩圈至 AI 编程全线）、信源哨兵 `tools/sentinel.js` + 新 skill `warco-dispatch`（战报）与 `warco-herald`（扇出）、支援/赞助位配置化；新增 `AUTOMATION.md` 自动化手册。老库升级：`node tools/migrate-2026-08-30.js` |
| 2026-08-27 | 种子数据重建为线上快照（53 条） |
| 2026-08-28 | 种子数据扩至 81 条（补模型军备线与并购前后缺环）；新增 warco-scout 侦察 skill（沉淀信源清单） |
| 2026-08-28 | **全站文案改版定稿**：正史·杂志特稿体 / 野史·红后终端体，库内档案全量重写（84 条） |
| 2026-08-28 | 全站调性统一：主标语定为「AI一日，人间一年」，署名立 CURSOR WARCO 呼号 |
| 2026-08-29 | 立档术语改版：成对线索的「开闸/拉闸」改为病毒学词系「检出/灭活」（tag 拉闸→灭活；开闸/拉闸降级为叙述动词） |
| 2026-08-29 | 行文纪律定稿：档案正文禁用「见 YYYY-MM-DD 档案」式文字引用与跨档案窗口天数计算，关联全部交给 series；库内 89 条全量清理（约 70 处） |

---

## 10. 已定决策与禁忌（AI 动手前必读）

**内容侧**

1. 文体已定稿（正史特稿体 / 野史红后体），**不要再提议换文体**；旧文案里的「战地记者按」一律作古，收尾用「系统备注：」。
2. 录入永远人工。AI 只交草稿；未经明确授权不写库、不删库、不改线上数据。
3. 图片永远站长自备，AI 不找图、不生成配图（例外：反爬拦截时按第 5 节做「公告存证卡」本地渲染）。
4. 正史数字必须有出处；查不到就模糊化，禁止编造精确数字。野史可演绎语气，不可虚构事件。
5. 术语纪律不可破（叙述用情报术语，黑话加「」）；唯一豁免区是战地手记。
6. `date` 记事件发生日；series ≥2 条才开；灭活档案 tag 固定「灭活」；检出/灭活标题用专用格式（「开闸/拉闸」是 2026-08-29 前的旧词，只能作叙述动词）。
7. 记者立场：不站队、不批判、仅作记录。任何新增站内文案都不得破坏这个立场。

**技术侧**

8. **零依赖原则**：不引入 npm 包，只用 Node 内置模块（这是项目的身份之一）。
9. `/admin` 与 `/admin.html` 恒为 404，后台入口只走隐藏路径；不要在任何公开文案、提交信息、截图里泄露真实 `ADMIN_PATH`。
10. 访客日志只存 IP 哈希，不落明文；渲染访问者可控字段必须转义。
11. 前台样式/脚本全部内联在 `index.html`（单文件哲学）；SSR 输出与客户端 `render()` 保持同构，改卡片结构时两边都要改（`server.js` 的 SSR 段 + `index.html`）。
12. 红白黑三色基调不破；新 UI 元素优先复用现有 CSS 变量与 mono/serif 字体气质。
13. 数据库 schema 以 `server.js` 初始化建表为准，不做运行时自动迁移（2026-08-27 已明确 revert 过一次）。

**世界观侧**

14. 「本系统不删除记录」是红后的人设——事件的后续发展靠新立档案并用 series 串联，不靠悄悄改写旧档案（后台编辑用于修错别字/补信源，不用于改写叙事）。
15. 新增年份记得补 `PHASES` 表（见 2.4），否则前台显示「PHASE-? 观测中」。

**情报站侧（2026-08-30 定稿）**

16. **不教唆红线**：套利 / 漏洞类内容全站只记录已公开的观测与官方处置（检出 / 灭活 / 存活天数），不写操作步骤、不出教程；作战室与数据页均带「不构成操作建议」声明。这是「记录不教唆」立场的产品化延伸，任何新功能不得越线。
17. **变现护栏**：免费站永远保留全部内容——将来若做付费，只收「时效与便捷」的钱（推送 / 简报），不拆档案墙；「补给线」赞助位由站长把关，与立场冲突的灰产（中转站 / 拼车类）不接，赞助输出一律 `rel=sponsored`。
18. **发布权永远在人**：agent 只能投草稿态（收件箱 / 编辑部 draft / drafts 文件），档案、战报、特稿的发布键与对外账号的发送键都在站长手里（AUTOMATION.md 第 3 节「人工闸口」）。
19. 战报体已定稿（开篇特稿腔总评 + 字段化区块 + 「下周观察」收尾，见 warco-dispatch skill），不再重开讨论；战报期号即永久 URL（`/w/:issue`），发布后不改号。

---

## 11. 待补与已知事项

- [ ] 「4365」取名的完整心证未留档（见第 1 节），站长想起来请补。
- [ ] **线上库全删全增待执行（最后一次）**：`drafts/full-rewrite-2026-08-29.sql`（独立自包含，一次性带上检出/灭活改词 + 新增 5 条，合计 89 条；其数据源 seed.js 已退役删除，SQL 文件本身完整可用）。服务器上 git pull → 备份库 → `sudo -u umbrella sqlite3 data/chronicle.db ".read drafts/full-rewrite-2026-08-29.sql"` → `systemctl restart umbrella4365`（SQL 文件头有完整注释）。注意全删全增会重排 /ev/:id，旧分享链接会指向新档案。执行后勾掉本项并删除该 SQL。
  **⚠ 时序硬约束（2026-08-29 SEO 改造后）**：本项必须在向搜索引擎提交 sitemap / 开百度推送**之前**执行完毕（完整时序见 `GROWTH.md` 第 0 节）；此后档案 id 视为永久 URL，不再全删全增，修订走后台逐条编辑或线上 API。
- [ ] **新线索记得配聚合页**：后台录入用了新的 series 名后，把 slug + 搜索友好标题 + 导语补进 `server.js` 的 `SERIES_PAGES`（未配置会回退中文 URL，能用但搜索表现打折）。
- [ ] 现役 series 清单（4.4 节）与线索分布（8 节）以线上 DB 为准，本文档记录的是 2026-08-29 口径。
- [ ] **2026-08-30 情报站升级待部署**：按 `drafts/launch-runbook-2026-08-30.md` 一次跑完（备份 → git pull → 全删全增 → 迁移 → 重启 → 补给表初始化 → 创刊内容发布 → 站长平台 → 哨兵定时 → 社区首发）。执行完勾掉本项与上面的全删全增项。
- [ ] **数据页 curated 维护点**：新版本发布 / 新一轮融资 → 立档之外记得补 `server.js` 的 `VERSIONS` / `FUNDING` 常量各一行（部署生效）；模型上架 / 停供 / 调价 → 后台「补给线」改行。
- [ ] **渠道注册**（注册完把链接填进后台「系统 → 频道与支援链接」）：TG 频道、X 账号、即刻、公众号（可选）、爱发电（可选）。
